<h1>search result</h1>
<h3><%= params[:q] %></h3>
<table>
	<% @via_pinners_count.sort_by{|k,v| v}.reverse.each do |k, v| %>
		<tr>
			<td><%= k %></td>
			<td><%= pluralize(v, 'pin') %></td>
		</tr>
	<% end %>
</table>